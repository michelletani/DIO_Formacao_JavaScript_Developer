const funcoes = require('./aula_01');

const pessoa = {
    nome: 'Michelle',
};

const { nome } = pessoa;

/*Faz a mesma coisa que a linha acima:

const nome = pessoa.nome; 
*/

console.log(funcoes);